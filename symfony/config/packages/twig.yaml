twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes: []
    globals:
        app_name: 'MERISU'
        # Rappel des codes de démonstration sur l'écran de connexion.
        # À laisser à 0 dès que l'application est joignable.
        show_demo_accounts: '%env(bool:MERISU_SHOW_DEMO_ACCOUNTS)%'
